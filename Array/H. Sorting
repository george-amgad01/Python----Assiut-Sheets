N = int(input())
arr = list(map(int, input().split()))
for i in range(N):
    mini = i
    for x in range (i+1 , N):
        if arr[x] < arr [mini] :
            mini = x
    arr[i],arr[mini] = arr[mini],arr[i]
print(*arr)
